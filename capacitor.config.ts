import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.newicon.bluesky',
  appName: 'bluesky',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
